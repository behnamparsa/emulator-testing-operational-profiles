name: Pipeline Manager

on:
  workflow_dispatch:
    inputs:
      chunk_size:
        description: URLs per shard
        required: false
        default: '10'
      max_parallel:
        description: Max shard pipelines in parallel
        required: false
        default: '3'
      url_list_path:
        description: Path to master URL list CSV
        required: false
        default: 'data/processed/URL_List.csv'

permissions:
  contents: write
  actions: read

jobs:
  prepare-shards:
    runs-on: ubuntu-latest
    outputs:
      shard_matrix: ${{ steps.build.outputs.shard_matrix }}
      shard_count: ${{ steps.build.outputs.shard_count }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Validate URL list exists
        run: |
          test -f "${{ github.event.inputs.url_list_path || 'data/processed/URL_List.csv' }}"

      - name: Build shard files and matrix
        id: build
        run: |
          python scripts/split_url_list.py \
            --input "${{ github.event.inputs.url_list_path || 'data/processed/URL_List.csv' }}" \
            --chunk-size "${{ github.event.inputs.chunk_size || '10' }}" \
            --out-dir shards

          python - <<'PY'
          import json, os
          from pathlib import Path
          shard_dir = Path('shards')
          shard_files = sorted([p.name for p in shard_dir.glob('shard_*.csv')])
          matrix = [{"shard_file": name, "shard_id": name[:-4]} for name in shard_files]
          with open(os.environ['GITHUB_OUTPUT'], 'a', encoding='utf-8') as out:
              out.write(f"shard_matrix={json.dumps(matrix)}\n")
              out.write(f"shard_count={len(matrix)}\n")
          print(json.dumps(matrix, indent=2))
          PY

      - name: Upload shard definitions
        uses: actions/upload-artifact@v4
        with:
          name: shard-definitions
          path: shards/

  run-shards:
    needs: prepare-shards
    strategy:
      fail-fast: false
      max-parallel: ${{ fromJSON(github.event.inputs.max_parallel || '3') }}
      matrix:
        include: ${{ fromJSON(needs.prepare-shards.outputs.shard_matrix) }}
    uses: ./.github/workflows/pipeline-shard.yml
    with:
      shard_file: ${{ matrix.shard_file }}
      shard_id: ${{ matrix.shard_id }}
    secrets: inherit

  aggregate:
    needs: run-shards
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Download shard artifacts
        uses: actions/download-artifact@v4
        with:
          path: downloaded_artifacts
          pattern: shard-*
          merge-multiple: false

      - name: Aggregate shard CSVs
        run: |
          python scripts/aggregate_results.py \
            --input-dir downloaded_artifacts \
            --output-dir aggregated_workdir

      - name: Build final MainDataset
        env:
          ROOT_DIR: ${{ github.workspace }}/aggregated_workdir
        run: |
          test -f aggregated_workdir/run_per_style_v1_stage3.csv
          test -f aggregated_workdir/run_workload_signature_v3.csv
          python -m pipeline.build_total_dataset

      - name: Persist final merged outputs into repo
        run: |
          mkdir -p data/processed data/study_exports
          cp aggregated_workdir/MainDataset.csv data/processed/MainDataset.csv
          cp aggregated_workdir/MainDataset.csv data/study_exports/MainDataset.csv

          cp aggregated_workdir/verified_workflows_v16.csv data/study_exports/verified_workflows_v16.csv
          cp aggregated_workdir/run_inventory.csv data/study_exports/run_inventory.csv
          cp aggregated_workdir/run_metrics_v16_stage3_enhanced.csv data/study_exports/run_metrics_v16_stage3_enhanced.csv
          cp aggregated_workdir/run_steps_v16_stage3_breakdown.csv data/study_exports/run_steps_v16_stage3_breakdown.csv
          cp aggregated_workdir/run_per_style_v1_stage3.csv data/study_exports/run_per_style_v1_stage3.csv
          cp aggregated_workdir/run_workload_signature_v3.csv data/study_exports/run_workload_signature_v3.csv

      - name: Commit merged dataset and study exports
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/processed/MainDataset.csv data/study_exports
          git diff --cached --quiet || git commit -m "Update sharded dataset + study CSV exports"
          git push

      - name: Upload aggregated outputs
        uses: actions/upload-artifact@v4
        with:
          name: final-aggregated-results
          path: |
            aggregated_workdir/MainDataset.csv
            aggregated_workdir/verified_workflows_v16.csv
            aggregated_workdir/run_inventory.csv
            aggregated_workdir/run_metrics_v16_stage3_enhanced.csv
            aggregated_workdir/run_steps_v16_stage3_breakdown.csv
            aggregated_workdir/run_per_style_v1_stage3.csv
            aggregated_workdir/run_workload_signature_v3.csv
            aggregated_workdir/aggregate_manifest.json
