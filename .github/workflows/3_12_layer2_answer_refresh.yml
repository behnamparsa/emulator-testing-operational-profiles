name: Layer 2 Answer Refresh (Deprecated)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "Layer 2 has been retired."
          echo "Use 3_00_profile_refresh_orchestrator.yml, which now runs Layer 1 and regeneration only."
