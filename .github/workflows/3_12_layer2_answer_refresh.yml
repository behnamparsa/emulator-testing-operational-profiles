name: Layer 2 Answer Refresh (Deprecated)

on:
  workflow_dispatch:
  workflow_call:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Layer 2 is deprecated. The single-layer flow now validates and refreshes directly in workflow 3_11."
